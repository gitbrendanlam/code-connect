import React from 'react';
import Layout from '../components/Layout';
import CTA from '../components/CTA';

const Home = () => {
  return (
    <Layout>
      <CTA />
    </Layout>
  );
};

export default Home;
